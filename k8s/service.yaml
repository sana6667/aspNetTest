apiVersion: v1
kind: Service
metadata:
  name: aspnet-app
  namespace: app
spec:
  selector:
    app: aspnet-app
  ports:
    - name: http
      port: 80          # внешний порт сервиса
      targetPort: 80    # порт в контейнере — теперь правильно
  type: LoadBalancer
